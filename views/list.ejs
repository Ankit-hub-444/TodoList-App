<%- include("header"); -%>

    <div class="box" id="heading">
      <h1><%= listTitle %></h1>
    </div>

    <div class="box" >

        <% newListitems.forEach((newListitem) => { %>

          <form  action="/delete" method="post">
            <div class="item">
              <input onChange="this.form.submit()" value="<%= newListitem._id %>" name="checkeditem" type="checkbox"  >
              <p><%= newListitem.name %></p>
            </div>
            <input type="hidden" name="listName" value="<%= listTitle %>"></input>
          </form>

      <%  }); %>


             <form class="item" action="/" method="post">

                 <input  type="text" name="newItem" placeholder="New Item" autocomplete="off" >
                 <button  type="submit" name="list" value=<%= listTitle %>>+</button>

             </form>
      </div>







<% /* %>
<% if(kindofDay==="Wednesday"||kindofDay==="Sunday"){ %>
        <h1 style="color:purple" >It's a <%=kindofDay %> Todo list</h1>
    <% }else{ %>
    <h1 style="color: blue">It's a <%=kindofDay %> Todo list</h1>
<%  }%>
<% */ %>

<%- include("footer"); -%>
